
<%= form_for post do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %><br>

    <%= f.label :content %>
    <%= f.text_field :content %><br>

Choose a category:
    <%= f.collection_check_boxes :category_ids, Category.all, :id, :name %>
<br>
Or Create a New Category:
    <%= f.fields_for :categories, post.categories.build do |t| %>
        <%= t.text_field :name %>
    <% end %>
    
    <%= f.submit %>

<% end %>